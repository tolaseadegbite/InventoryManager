<%= render 'layouts/modal_dialog' do %>
  <div data-action="turbo:submit-end->dialog#submitEnd">
    <h1 class="font-semibold text-xl sm:text-xl md:text-xl p-5 border-b">
      Manage Category Permissions for <%= @inventory_user.user.profile&.name || @inventory_user.user.email %>
    </h1>
    <%= render 'form' %>
  </div>
<% end %>